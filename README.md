# Description

This is a [Gatsby](https://www.gatsbyjs.org/) static site.

This site is hosted at **[TODO]**.

## Getting started

Install gatsby cli and repo dependencies:
```shell
npm -g i gatsby-cli
npm install
```

Run the development server:
```shell
gatsby develop
```

Build and run the production version locally:
```shell
gatsby build && gatsby serve
```

## Deploy the site

We currently host landing pages on [Cloudflare pages](https://pages.cloudflare.com/).
